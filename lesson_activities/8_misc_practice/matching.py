#=======================
# TODO
# Write a function 'match110' that takes a list of '0's and '1's as an argument
# It should return True if the argument list contains the sequence [..., '1', '1', '0', ...] and return False otherwise




#=======================
# TODO
# Write a function 'matchPattern' that takes an arbitrary list and a pattern list as arguments
# It should return True if the first list contains a contiguous sub-sequence of elements that is the same as the contents of the pattern list.

