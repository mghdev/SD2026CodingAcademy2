# Read the grid-coordinates discussion.
#
# We have a square of side length 50 with an origin point (0,0) in the bottom left, x-axis to the right and y-axis up.
# Imagine that we split this region into a 5x5 grid of square cells with side length 10.

#=======================
# TODO
# Write a pair of functions 'yToRow' and 'xToCol' that take fine-grained point coordinates as arguments and return the corresponding row / column number.
# Call your functions at least twice each with test arguments and print the return values to verify that they are correct.
def yToRow(y):
    return 0

def xToCol(x):
    return 0



#=======================
# TODO
# Imagine the grid is indexed like this:
#   20  21  22  23  24
#   15  16  17  18  19
#   10  11  12  13  14
#   5   6   7   8   9
#   0   1   2   3   4
# Write a function that takes the coordinates of a point and returns the index for the corresponding grid square
# Call your function at least twice with test arguments and print the return values to verify that they are correct.
def xyToIndex(x,y):
    return 0


